#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    ll n;
    cin >> n;
    vector<ll> a(n), b(n);
    vector<bool> useda(n), usedb(n);
    for(ll &x: a) cin >> x;
    for(ll &x: b) cin >> x;
}