#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
typedef long long ll;
void solve(){
    ll n;
    cin >> n;
    vector<ll> a(n);
    ll min = __LONG_LONG_MAX__
    for(auto &x : a){
        cin >> x;
        min = min(min, x);
    } 
    if(a[0] == min){

    } else{

    }
}
int main(){
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    ll t;
    cin >> t;
    while(t--){
        solve();
    }
}