#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;


void solve(){
    ll n, s, m, x, y, last;
    cin >> n >> s >> m;
    while(n--){
    }
}
int main(){
    ll t;
    cin >> t;
    while(t--) solve();
}