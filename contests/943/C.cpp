#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
void solve(){
    ll n;
    vector<ll> x(n-1), a(n);
    for (ll i = 0; i < n-1; i++)
    {
        cin >> x[i];
    }
    
    ll k = 0; 
    ll j = 0; 

    for (ll i = 0; i < j && j-1 < n; i++) {}
}
int main(){
    std::cin.tie(nullptr);
    std::ios_base::sync_with_stdio(false);
    ll t; cin >> t;
    while (t--)
    {
        solve();
    }
    
}
