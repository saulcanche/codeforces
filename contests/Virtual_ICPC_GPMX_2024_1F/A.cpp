#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    ll n, x; 
    cin >> n;
    map<ll, vector<ll>> freq;
    for(ll i = 0; i < n; i++){
        cin >> x;
        freq[x].push_back(i);
    }
    ll q; 
    cin >> q;
    for(ll i = 0; i < q; ++i){
        for(x)
    }
}
