#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;
int main(){
    ll n, q, l, r;
    cin >> n;
    vector<ll> a(n);
    for(auto &x : a) cin >> x;
    mxl[0] = 0;
    while(q--){
        cin >> l >> r;

    }
}